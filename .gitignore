# Credentials — never commit
.env
gmail_credentials.json
gmail_token.json

# Runtime data — not source code
fiserv_processed_emails.json
fiserv_sheets_id.json

# Database — contains real user data
*.db
*.db-shm
*.db-wal

# Logs — may contain phone numbers
*.log

# Python runtime
venv/
__pycache__/
*.pyc
*.pyo

# Backups — not source code
backups/
*.bak

# OS
.DS_Store
